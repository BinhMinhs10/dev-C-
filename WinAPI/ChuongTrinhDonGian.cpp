#include <windows.h>
int WINAPI WinMain(HINSTANCE hInstance,HINSTANCE hPrevInstance, 
                LPSTR IpCmdLine, int nCmdShow)
{
	MessageBox(NULL, "your sourcecode is wrong", "Note",MB_OK);
	return 0;
}
