#include <stdio.h>
#include <conio.h>
#include <math.h>
int giaithua(int n){
	  int i,n,a=1;
      for(i=1;i<=n;i++)
	  	  	a=a*i;
	  return a;
};
main()
{
	int i,n;
	float s=0,x;
	do{
		printf("nhap so radien x: ");
		scanf("%f",&x);
	}whlie(x<=0||x>=10);
	while(tg<)
	
}
